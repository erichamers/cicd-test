on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: erichamers/cicd-templates/.github/workflows/hello.yml@main
    with:
      environment: ${{ github.ref_name == 'main' && 'PRD' || github.ref_name == 'staging' && 'STG' || 'DEV' }}
    secrets: inherit